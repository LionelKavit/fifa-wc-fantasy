# Screenshots

Drop the PNGs below into **this folder** (`docs/images/`) and they'll be embedded in the
main README and the docs. Capture on a **wide desktop viewport (~1400px), dark theme, no
browser chrome**; original styling only (no FIFA marks). Note the teams/state shown so
captions stay accurate.

| File | What it shows |
|---|---|
| `predictor-hero.png` | **Hero.** The full Bracket Predictor (Knockouts tab): the "Need help filling your bracket?" settings box, the bracket columns Round of 32 → Final with per-pick head-to-head %, and the "Ask the Analyst" panel on the right. |
| `build-your-bracket-with-ai.png` | The build/autofill box — pool size, risk level, "Finish my bracket" — illustrating the AI autofill step. |
| `analyst-bracket.png` | A close-up of the "Ask the Analyst" panel: a bracket Q&A ("How's my bracket looking?") and a head-to-head pick question. |
| `group-dashboard.png` | The Group stage tab: group cards with standings, per-team status (Through / In contention / 3rd-place race / Out) and Next Round %. |
| `team-detail.png` | A team detail dialog (a team's results + qualifying status). |

Capture on a wide desktop viewport (~1400px), dark theme, no browser chrome; original
styling only (no FIFA marks).
