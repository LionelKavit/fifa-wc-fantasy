import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Self-contained production server (.next/standalone), matching the reference app.
  output: "standalone",
};

export default nextConfig;
